# conda env create --file environment.yml
name: cookiecutter_personal
channels:
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - fs
  - jupyter
  - jupyterlab
  - pathlib
  - pip
  - pyprojroot
  - python=3.12.0
  - pip:
    - pyhere
    